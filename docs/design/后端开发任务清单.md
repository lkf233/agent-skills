# 后端开发任务清单

## 使用说明
- 本清单用于后续后端迭代跟踪。
- 每完成一项任务，将对应的 `- [ ]` 改为 `- [x]`。
- 涉及架构或数据库表结构变更时，同步更新技术文档并补充增量 DDL。

## 阶段 0：架构重规划与契约冻结
- [x] 生成并维护后端 API 文档
- [x] 完成 Agent 平台 V2 的 ERD 文档
- [x] 完成 V3-V8 增量 DDL 规划
- [x] 冻结 Agent/资产/对话接口契约（OpenAPI）
- [x] 统一错误码分层并发布错误码表

## 阶段 1：资产底座（Skills/Tools/知识库）
- [x] 落地 V3-V5 迁移脚本
- [ ] 完成 V3-V5 数据库升级验证
- [x] 实现知识库创建与列表查询接口
- [x] 明确检索链路（向量检索+关键词检索+RRF+ReRank）
- [x] 实现知识库文件上传与解析状态接口
- [x] 实现异步入库任务与向量化调度
- [x] 接入真实Embedding并升级pgvector存储
- [ ] 实现 Skill 上传与版本管理接口
- [x] 实现 Tool 配置与连通性测试接口
- [x] 支持自定义认证请求头并查询远程MCP工具列表
- [ ] 完成资产域权限校验与伪删除规则

## 阶段 2：Agent 配置中心（先完成可创建与可绑定）
- [x] 增量 DDL：为 `agent` 新增 `system_prompt` 字段并补充文档
- [x] 实现 Agent 创建接口（名称、描述、systemPrompt）
- [x] 实现 Agent 基础信息更新接口（名称、描述、systemPrompt、头像）
- [x] 实现 Agent 绑定 Tools 接口（仅允许绑定当前用户可用工具）
- [x] 实现 Agent 绑定知识库接口（仅允许绑定当前用户可用知识库）
- [x] 实现 Agent 详情聚合接口（基础信息+已绑定工具+已绑定知识库）
- [ ] 完成资产域权限校验与伪删除规则补齐（Agent/Tool/KB 统一）

## 阶段 3：临时对话链路（创建Agent前可预览）
- [ ] 增量 DDL：新增 `preview_session`、`preview_message`、`tool_call_log`、`rag_recall_log`
- [ ] 实现临时会话创建接口（请求携带草稿配置：名称/描述/systemPrompt/toolIds/kbIds）
- [ ] 实现临时消息发送接口（仅基于草稿配置执行，不落 `agent` 正式记录）
- [ ] 实现临时会话 SSE 流式输出（事件：token/tool_call/rag_recall/done/error）
- [ ] 实现临时会话工具调用约束（仅可调用请求中声明且校验通过的 MCP Tool）
- [ ] 实现临时会话知识库召回约束（仅在请求中声明的 kbIds 范围做向量+关键词双路召回）
- [ ] 实现临时会话 RRF 融合与 ReRank 精排
- [ ] 实现临时会话日志落库（工具调用日志、召回日志、失败原因）

## 阶段 4：正式 Agent 对话链路（创建后）
- [ ] 增量 DDL：新增 `conversation_message` 并完善 `conversation` 索引
- [ ] 实现基于 Agent 的会话创建接口（仅允许 `status=ACTIVE` 的 Agent）
- [ ] 实现正式消息发送与 SSE 流式输出接口
- [ ] 实现正式会话消息历史分页接口
- [ ] 实现会话关闭与归档能力
- [ ] 实现运行时策略路由（模型自动判断：直接回复 / RAG / MCP）
- [ ] 实现正式链路工具调用与知识库检索审计日志
- [ ] 完成临时链路与正式链路隔离校验（数据隔离、权限隔离、指标隔离）

## 阶段 5：Agent 生命周期与发布治理
- [ ] 实现 Agent 发布接口（DRAFT -> ACTIVE）与发布前校验
- [ ] 实现 Agent 归档接口（ACTIVE -> ARCHIVED）与会话影响控制
- [ ] 实现 Agent 配置版本快照（用于回滚与问题追踪）
- [ ] 实现发布/归档审计日志与操作人记录

## 阶段 6：工程化与安全治理
- [ ] 移除硬编码密钥并改为环境变量注入
- [ ] 改造数据库凭据配置并支持多环境
- [ ] 建立后端测试基线与 CI 校验流程
- [ ] 建立关键指标监控与告警基线
- [ ] 建立性能压测基线与容量评估报告

## 前后端协同里程碑
- [ ] M1：阶段 1 完成后启动前端资产管理页面联调
- [ ] M2：阶段 2 完成后启动前端 Agent 配置页面联调
- [ ] M3：阶段 3 完成后启动前端临时对话预览页面联调
- [ ] M4：阶段 4 完成后启动前端正式对话页面联调
- [ ] M5：阶段 5 完成后启动前端 Agent 发布与版本管理页面联调
